1. Backend handler

Python (for windows with bash): cd python-service -> source .venv/Scripts/activate -> uvicorn app.main:app --reload --port 8000
Node.js: cd node-service -> npm run batch

2. Client image check

Python (for windows with bash): cd python-service -> source .venv/Scripts/activate -> uvicorn app.main:app --reload --port 8000
Next.js: cd web -> npm run dev -> http://localhost:3000